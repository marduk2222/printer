[General]
; Printer setup 對接位置（同 repo 的 printer web 專案）
Protocol = REST
RestUrl = http://localhost:5062

; printer web 目前沒有多 DB 概念，此欄保留供日後擴充
RestDb = printer

; 登入的員工帳號（對應 /api/auth），預設管理員 admin/admin
RestLogin = admin
RestPassword = admin

; 最近使用的 Partner Id（VM 在「新增並回報」成功後寫回）
Partner = 0

; 公司統一編號（目前 UI 未使用，保留）
TaxID =
