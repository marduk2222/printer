@model printer.Models.InvoicePrintViewModel
@{ Layout = null; var primary = !string.IsNullOrEmpty(Model.Settings.PrimaryColor) ? Model.Settings.PrimaryColor : "#7c3aed";
   var partner = Model.Invoice.Partner; var buyerName = !string.IsNullOrWhiteSpace(partner?.InvoiceTitle) ? partner!.InvoiceTitle : partner?.Name;
   var buyerTaxId = !string.IsNullOrWhiteSpace(partner?.InvoiceTaxId) ? partner!.InvoiceTaxId : partner?.Vat; }
<!DOCTYPE html><html lang="zh-Hant"><head><meta charset="utf-8" /><title>帳單 - @Model.Invoice.InvoiceNumber</title>
<style>:root{--p:@primary}*{box-sizing:border-box;margin:0;padding:0}body{font-family:"Noto Sans TC",sans-serif;color:#1f2937;background:#f3f4f6;font-size:13px}
.no-print{display:flex;gap:8px;padding:12px;background:#fff;border-bottom:1px solid #e5e7eb}.no-print button{padding:8px 22px;border:1px solid var(--p);background:#fff;color:var(--p);cursor:pointer;font-size:14px}.btn-print{background:var(--p) !important;color:#fff !important}
.page{max-width:820px;margin:24px auto;background:#fff;padding:24px 28px}
.brand{font-size:22px;color:var(--p);font-weight:600;margin-bottom:14px}
.grid-cells{display:grid;grid-template-columns:repeat(4,1fr);border:1px solid var(--p);margin-bottom:18px}
.cell{padding:12px 14px;border-right:1px solid var(--p);background:#f5f3ff}
.cell:last-child{border-right:none}
.cell .lbl{font-size:10px;color:var(--p);letter-spacing:3px;margin-bottom:4px}
.cell .v{font-size:13px;font-weight:600;color:#1f2937}
.cell .v.big{font-size:18px;color:var(--p)}
table{width:100%;border-collapse:collapse;margin-bottom:18px}th{padding:10px 12px;background:var(--p);color:#fff;font-size:12px;text-align:center}th:first-child{text-align:left}
td{padding:10px 12px;border-bottom:1px solid #e5e7eb;font-size:12px;text-align:center}td.left{text-align:left}td.right{text-align:right}
.totals{display:flex;justify-content:flex-end;margin-bottom:14px}.totals-card{width:300px;border:1px solid var(--p)}.totals-row{display:flex;justify-content:space-between;padding:8px 16px;font-size:13px;border-bottom:1px solid #e5e7eb}.totals-row.grand{background:var(--p);color:#fff;font-weight:700;padding:12px 16px;font-size:15px;border:none}
.foot{padding-top:12px;border-top:1px dashed #e5e7eb;font-size:11px;color:#6b7280;display:grid;grid-template-columns:1fr 1fr;gap:18px}.foot h6{color:var(--p);margin-bottom:4px}
@@media print{body{background:#fff}.no-print{display:none !important}.page{margin:0;max-width:none;-webkit-print-color-adjust:exact;print-color-adjust:exact}}@@page{size:A4;margin:10mm}
</style></head><body>
<div class="no-print"><button class="btn-print" onclick="window.print()">列印</button><button onclick="history.back()">返回</button></div>
<div class="page">
<div class="brand">@(Model.Settings.CompanyName ?? "公司名稱") <span style="font-size:11px;color:#6b7280;font-weight:400;">·　帳　單</span></div>
<div class="grid-cells">
    <div class="cell"><div class="lbl">編號</div><div class="v big">@Model.Invoice.InvoiceNumber</div></div>
    <div class="cell"><div class="lbl">開立日期</div><div class="v">@Model.Invoice.CreatedAt.ToString("yyyy/MM/dd")</div></div>
    <div class="cell"><div class="lbl">客戶</div><div class="v">@buyerName</div></div>
    <div class="cell"><div class="lbl">統一編號</div><div class="v">@(buyerTaxId ?? "—")</div></div>
    <div class="cell"><div class="lbl">客戶地址</div><div class="v" style="font-size:11px">@(partner?.Address ?? "—")</div></div>
    <div class="cell"><div class="lbl">客戶電話</div><div class="v">@(partner?.Phone ?? "—")</div></div>
    <div class="cell"><div class="lbl">期間起</div><div class="v">@Model.Invoice.PeriodStart.ToString("yyyy/MM/dd")</div></div>
    <div class="cell"><div class="lbl">期間迄</div><div class="v">@Model.Invoice.PeriodEnd.ToString("yyyy/MM/dd")</div></div>
</div>
<table><thead><tr><th style="width:34%">項目</th><th>黑白</th><th>彩色</th><th>大張</th><th>月租</th><th>張數費</th><th style="text-align:right;">小計</th></tr></thead><tbody>@foreach(var it in Model.Invoice.Items){<tr><td class="left">@it.Description</td><td>@it.BlackPages.ToString("N0")</td><td>@it.ColorPages.ToString("N0")</td><td>@it.LargePages.ToString("N0")</td><td class="right">@it.MonthlyFee.ToString("N0")</td><td class="right">@it.PageFee.ToString("N0")</td><td class="right">$@it.Subtotal.ToString("N0")</td></tr>}</tbody></table>
<div class="totals"><div class="totals-card"><div class="totals-row"><span>未稅</span><span>$@Model.Invoice.TotalAmount.ToString("N0")</span></div><div class="totals-row"><span>稅 (5%)</span><span>$@Model.Invoice.TaxAmount.ToString("N0")</span></div><div class="totals-row grand"><span>含稅總計</span><span>$@Model.Invoice.GrandTotal.ToString("N0")</span></div></div></div>
<div class="foot"><div>@if(!string.IsNullOrEmpty(Model.Settings.BankName)){<h6>匯款</h6><p>@Model.Settings.BankName @Model.Settings.BankBranch / @Model.Settings.BankAccount</p>}</div><div>@if(!string.IsNullOrEmpty(Model.Settings.FooterNote)){<h6>備註</h6><p>@Model.Settings.FooterNote</p>}</div></div>
</div></body></html>
